---
import Badge from "./Badge.astro";
import type { Service } from "../data/services";
import type { Lang } from "../i18n";
import { t } from "../i18n";
import { useT } from "../i18n/ui";
interface Props {
  service: Service;
  lang: Lang;
}
const { service, lang } = Astro.props;
const T = useT(lang);
const isLink = service.status === "live" && service.url;
const accent = `var(--c-${service.accent ?? "mint"})`;
const cta = isLink ? T("cta.external") : T("cta.view");
---
<a
  class="proj"
  href={isLink ? service.url : "#apps"}
  {...isLink ? { target: "_blank", rel: "noopener" } : {}}
  style={`--accent:${accent}`}
>
  <span class="proj__swatch"></span>
  <div class="proj__top">
    <span class="eyebrow">project</span>
    <Badge status={service.status} />
  </div>
  <h3 class="proj__title">{t(service.name, lang)}</h3>
  <p class="proj__desc">{t(service.desc, lang)}</p>
  <span class="proj__cta">{cta} →</span>
</a>

<style>
  .proj {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 22px;
    background: var(--card);
    border: var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    transition: transform 0.12s, box-shadow 0.12s;
  }
  .proj:hover {
    transform: translate(-3px, -3px);
    box-shadow: 6px 6px 0 var(--ink);
  }
  .proj__swatch {
    height: 64px;
    margin: -22px -22px 6px;
    background: var(--accent);
    border-bottom: var(--border);
  }
  .proj__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .proj__title { font-size: 21px; font-weight: 700; letter-spacing: -0.01em; margin: 2px 0 0; }
  .proj__desc { color: var(--soft); font-size: 0.92rem; flex: 1; }
  .proj__cta { font-family: var(--font-mono); font-size: 0.8rem; color: var(--ink); margin-top: 4px; }
</style>
